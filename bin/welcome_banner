#!/usr/bin/env bash

hash figlet || (echo 'error: figlet not installed.' 1>&2)
hash lolcat || (echo 'error: lolcat not installed.' 1>&2)

figlet -f standard $(hostname) | lolcat -f --spread 8 | sudo tee /etc/motd
